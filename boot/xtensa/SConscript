# coding:utf-8
# *******************************************************************
# * Copyright 2021-2080 evilbinary
# * 作者: evilbinary on 01/01/20
# * 邮箱: rootdebug@163.com
# ********************************************************************
import os

Import('env')               

xtensa_abi_flags = ' -mlongcalls -mtext-section-literals -mabi=call0 '
linkflags=' $LDFLAGS' + xtensa_abi_flags + ' -nostdlib -nostartfiles -T boot/xtensa/link.ld'
cflags= '$CFLAGS' + xtensa_abi_flags + ' -fno-stack-protector  '
asflags= '$ASFLAGS' + xtensa_abi_flags
include=[
    '.',
    'include',
    '../../duck',
    '../../duck/platform/'+env['PLATFORM']
]



source = [
    'boot.o',
    'vectors.s',
    'init.c',
    'config.h'
    ]

if env.get('MYLIB'):
    source.append(env.get('MYLIB'))

if env.get('SINGLE_KERNEL'):
    boot_source=[
        'boot.s'
    ]
    source=[
        'init.c',
    ]
    cflags+= ' -DSINGLE_KERNEL '
    env.Object('boot.o',boot_source,ASFLAGS=asflags)
    env.Object('init.o',source,ASFLAGS=asflags + ' $CFLAGS',CPPPATH=include,
        CFLAGS=cflags,
        LINKFLAGS= linkflags)
else:
    env.GenerateKernelSize('config.h',['../../duck/init/kernel'])
    env.Program(target ='init.elf', source = source,
        CPPPATH=include,
        CFLAGS=cflags,
        ASFLAGS=asflags,
        LINKFLAGS= linkflags)

    env.Objcopy('init.bin','init.elf',OBJCOPYFLAGS='-O binary -S --strip-debug')